CREATE TABLE IF NOT EXISTS user_sessions (id BINARY(16) PRIMARY KEY, user_id BINARY(16) NOT NULL, refresh_token VARCHAR(255) NOT NULL, created_at DATETIME(6) DEFAULT CURRENT_TIMESTAMP(6) NOT NULL, expires_at DATETIME(6) NOT NULL, revoked_at DATETIME(6) NULL, CONSTRAINT fk_user_sessions_user_id__id FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE RESTRICT ON UPDATE RESTRICT);