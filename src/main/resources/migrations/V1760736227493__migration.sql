CREATE TABLE IF NOT EXISTS users (id BINARY(16) PRIMARY KEY, email VARCHAR(255) NOT NULL, password_hash VARCHAR(255) NOT NULL, `role` VARCHAR(20) NOT NULL, created_at DATETIME(6) DEFAULT CURRENT_TIMESTAMP(6) NOT NULL);
ALTER TABLE users ADD CONSTRAINT users_email_unique UNIQUE (email);